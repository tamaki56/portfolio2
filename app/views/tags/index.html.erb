<div class="tag-list-contents">
  <h3>TagsList</h3>
  <ul>
    <% @tags.each do |tag| %>
      <li>
        <%# タグの名前と、関連付く記事数を表示 %>
        <p><%= link_to "#{tag.name} (#{tag.photos.count}件 )", tag_path(tag) %></p>
      </li>
    <% end %>
  </ul>
</div>
